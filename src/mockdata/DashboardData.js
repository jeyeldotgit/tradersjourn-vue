export const DashboardMockData = {
  balance: 623.5,
  CumulativePNL: 123.5,
  winRate: 0.3435,
  mostTradedInstrument: "EURUSD",
};

// Computation
// balance = initialDeposit + (cumulativePNL - withdrawals)
// winrate = totalWins / (totalWins + totalLosses)
